# Question 3

问题3检验年龄、身高、体重、IVF、孕次、产次等因素在BMI之外是否带来稳定增益，并判断问题2的两组政策是否值得复杂化。

## 当前精简流程

- `nested_q3_audit.py`：读取工程内冻结的外层候选表；每个外层折只在该折训练数据内选择分组和时点，再到外层留出患者上评估，避免把外层测试结果用于选参。
- `../question123/run_q123.py`：统一执行审计、绘图和复现清单更新。

历史多因素探索、动态规划搜索、报告构建和重复稳健性脚本已在结果冻结后删除；保留的结构化结果用于核对模型增益、切点稳定性、检测误差和固定政策边界。

当前便于解释的建议为 BMI<35 第18周、BMI≥35 第22周。它是全数据开发后的固定政策内部稳定性建议，不能称为无偏独立测试结果；`nested_q3_audit.py`给出的外层审计用于估计该类建模流程的泛化边界，其中均值安全4/4、保守下界安全3/4。

精简结果位于 `../results/q3/`，统一复现请运行 `python scripts/run_all.py`。
